<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DevSecOps Engineer</title>
    <style>
        /* Global Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
            text-align: center;
        }

        /* Body Styling */
        body {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            background: linear-gradient(135deg, #1e3c72, #2a5298);
            color: white;
            padding: 20px;
        }

        /* Heading */
        h1 {
            margin-bottom: 20px;
            font-size: 2.2rem;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
        }

        /* Form Styling */
        form {
            background: rgba(255, 255, 255, 0.2);
            padding: 20px;
            border-radius: 10px;
            backdrop-filter: blur(10px);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            width: 350px;
        }

        input[type="text"] {
            width: 90%;
            padding: 10px;
            margin: 10px 0;
            border: none;
            border-radius: 5px;
            outline: none;
            font-size: 1rem;
        }

        input[type="submit"] {
            width: 100%;
            background: #ff6b6b;
            color: white;
            padding: 10px;
            font-size: 1rem;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: 0.3s ease;
        }

        input[type="submit"]:hover {
            background: #ff4757;
        }

        /* Table Styling */
        table {
            margin-top: 20px;
            width: 80%;
            max-width: 600px;
            border-collapse: collapse;
            background: white;
            color: black;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        th, td {
            padding: 12px;
            border-bottom: 1px solid #ddd;
        }

        th {
            background: #ff6b6b;
            color: white;
        }

        tr:hover {
            background: rgba(255, 107, 107, 0.2);
        }

        /* Link Styling */
        a {
            color: #ffd700;
            font-weight: bold;
            text-decoration: none;
        }

        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>

    <h1>Hello, I'm a DevSecOps Engineer</h1>

    <% if (locals.ids) { %>
        <p>
            <a target="_blank" href="http://localhost:8000/url/<%= ids %>">🎯 URL is Generated! Click Here</a>
        </p> 
    <% } %>

    <form action="/url" method="post">
        <label for="url">Enter URL:</label>
        <input type="text" id="url" name="url" placeholder="Enter URL">
        <input type="submit" value="Submit">
    </form>

    <% if (locals.urls && locals.urls.length) { %>
        <table>
            <tr>
                <th>Redirect URL</th>
            </tr>
            <% urls.forEach(url => { %>
                <tr>
                    <td><%= url.ReDirect %></td>
                </tr>
            <% }) %>
        </table>
    <% } %>

</body>
</html>
